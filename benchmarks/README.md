# Benchmark suite

The included cases are synthetic and test data plumbing, metrics, severity handling, anchor matching, and panel-duplication detection. They are not evidence that the reviewer skills match experts.

Production benchmark cases should use authorized manuscripts, independent expert labels, blinded model runs, adjudication, and per-domain reporting. Predictions are JSONL with one object per case.
