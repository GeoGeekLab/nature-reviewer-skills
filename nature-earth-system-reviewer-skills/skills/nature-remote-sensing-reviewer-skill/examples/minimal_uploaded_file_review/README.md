# Minimal uploaded-file review

This example shows the intended low-friction invocation:

```text
Use $nature-remote-sensing-reviewer to review the uploaded files.
```

The skill infers file roles, extracts readable text, determines material scope, searches reviewer memory, and produces a Nature-style peer-review-file report.
