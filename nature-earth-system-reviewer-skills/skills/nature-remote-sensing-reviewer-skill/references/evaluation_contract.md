# Evaluation contract

A review output should expose machine-readable concerns containing `issue_id`, `severity`, `text`, `anchors`, `reviewer_id`, and `confidence`. This enables benchmark scoring and duplicate detection. Narrative reports may follow, but should not be the only output in evaluation runs.
