# Repository Guide

This repository is a standalone Nature-style reviewer skill. `SKILL.md` is the executable instruction file; `references/` contains review gates; `reviewer_db/` contains abstracted reviewer-memory records; `templates/` contains output formats; and `scripts/` contains maintenance helpers.
