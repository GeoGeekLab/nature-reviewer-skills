# Evaluation contract

Evaluation output must include stable `issue_id`, calibrated `severity`, evidence-linked `text`, manuscript `anchors`, `reviewer_id`, and `confidence`. Polar evaluations should additionally record selected routes and whether research-responsibility checks were triggered. Measure issue recall, false positives, severity agreement, anchor coverage, routing accuracy and cross-referee duplication.
