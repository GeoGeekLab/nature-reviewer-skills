# Architecture

This package is an orchestrator, not an eighth foundational domain skill. It routes polar claims to 1-4 of the seven existing skills, adds polar-specific gates, assigns distinct reviewer roles, then deduplicates and synthesizes concerns. The shared runtime remains the source of validation, retrieval, extraction and evaluation behavior.
