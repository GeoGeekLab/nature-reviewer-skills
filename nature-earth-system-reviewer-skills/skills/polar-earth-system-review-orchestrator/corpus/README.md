# Corpus metadata

`index.csv` and `sources.jsonl` record the public sources used in distillation. `checksums.sha256` covers the metadata files only. Raw peer-review files are intentionally excluded.
