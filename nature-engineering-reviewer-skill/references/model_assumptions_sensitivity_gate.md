# Model Assumptions and Sensitivity Gate

For simulations, optimization, AI models and mechanistic models, separate:

- assumptions;
- fitted parameters;
- calibration data;
- validation data;
- sensitivity analysis;
- uncertainty propagation;
- prediction outside the calibration range.

Do not allow simulation to replace experimental validation unless the claim is explicitly simulation-only.
